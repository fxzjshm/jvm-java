apply plugin: 'java'
apply plugin: 'jacoco'

dependencies {
    testCompile "junit:junit:$junitVersion"
}

jacocoTestReport {
    reports {
        xml.enabled true
        html.enabled false
    }
}

check.dependsOn jacocoTestReport